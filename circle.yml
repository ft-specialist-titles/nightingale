test:
  pre:
    - bower i
  post:
    - git config user.name "circleci"
    - git config user.email "peter.mouland@ft.com"
deployment:
  production:
    branch: master
    commands:
      - ./node_modules/component-helper/bin/component release --version=current
machine:
  node:
    version: v0.10.33
